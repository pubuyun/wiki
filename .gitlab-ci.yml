# Job name has to be `pages`. See https://docs.gitlab.com/ee/user/project/pages/#how-it-works
image: node:22-alpine

stages:
    - build
    - deploy

pages:
    stage: build
    cache:
        - key:
              files:
                  - yarn.lock
          paths:
              - .yarn-cache/
    before_script:
        - echo 'yarn-offline-mirror ".yarn-cache/"' >> .yarnrc
        - echo 'yarn-offline-mirror-pruning true' >> .yarnrc
        - yarn install --frozen-lockfile
    script:
        - export NUXT_APP_BASE_URL="/${CI_PROJECT_NAME}/"
        - export NUXT_STUDIO="false"
        - yarn generate
        - rm -rf public
        - mkdir -p public
        - cp -a .output/public/. public/
    artifacts:
        paths:
            - public
    rules:
        - if: $CI_COMMIT_BRANCH != $CI_DEFAULT_BRANCH
          when: never
        - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
          when: always

sync_github:
    stage: deploy
    image: alpine/git
    script:
        - git remote add github "https://x-access-token:${GITHUB_TOKEN}@github.com/pubuyun/wiki.git"
        - git push github HEAD:main
    rules:
        - if: $CI_COMMIT_BRANCH != $CI_DEFAULT_BRANCH
          when: never
        - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
          when: on_success
