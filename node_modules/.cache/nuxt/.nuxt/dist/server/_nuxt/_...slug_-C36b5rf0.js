import { defineComponent, mergeProps, useSSRContext, ref, computed, watch, nextTick, unref, isRef, withCtx, createVNode, toDisplayString, openBlock, createBlock, Fragment, renderList, h, getCurrentInstance, toRaw, reactive, resolveComponent, defineAsyncComponent, Text, Comment, withAsyncContext } from "vue";
import { ssrRenderAttrs, ssrRenderAttr, ssrInterpolate, ssrRenderClass, ssrRenderComponent, ssrRenderList, ssrRenderSlot } from "vue/server-renderer";
import { AccordionRoot, AccordionItem, AccordionHeader, AccordionTrigger, AccordionContent } from "radix-vue";
import { pascalCase, kebabCase } from "C:/Users/wzh_z/Documents/wiki/node_modules/scule/dist/index.mjs";
import destr from "C:/Users/wzh_z/Documents/wiki/node_modules/destr/dist/index.mjs";
import { find, html } from "property-information";
import { toHast } from "minimark/hast";
import { d as useRuntimeConfig, u as useRoute, e as useAsyncData, c as createError } from "../server.mjs";
import { q as queryCollection } from "./client--Rr_NKjR.js";
const _sfc_main$4 = /* @__PURE__ */ defineComponent({
  __name: "Banner",
  __ssrInlineRender: true,
  props: {
    imgSrc: {},
    title: {}
  },
  setup(__props) {
    return (_ctx, _push, _parent, _attrs) => {
      _push(`<div${ssrRenderAttrs(mergeProps({ class: "grid leading-none" }, _attrs))}><img class="col-1 row-1 h-[90vh] w-full object-cover"${ssrRenderAttr("src", __props.imgSrc)} alt=""><svg class="top-border col-1 row-1 self-end" viewBox="0 -10 40 10" preserveAspectRatio="none"><path class="fill-secondary" d="M 0 0 L 0 -9 c 1.4 0 3.2 0.5 4.436 1.875 Q 7.8 -3.8 9.4 -4.8 Q 10.788 -5.629 12.2 -6 Q 14.4 -6.3 15.4 -5.5 C 20.795 -1.028 22 -5.5 26.2 -5.5 Q 29.7 -5.5 31.7 -2.3 Q 32.363 -1.158 33.4 0" transform="translate(0,0.1)"></path><path class="fill-primary-deep" d="M 0 0 V -9 c 0.3 0 1.473 0.275 3.585 2.585 q 2.067 1.96 4.483 0.805 q 1.642 -0.82 3.237 -0.836 q 1.509 0 2.779 1.04 c 4.291 3.395 4.703 -0.429 10.146 -0.422 Q 27.8 -5.9 30.3 -1.8 q 0.619 0.865 1.4 1.8" transform="translate(0,0.1)"></path><text x="2" y="-1"${ssrRenderAttr("textLength", __props.title.length > 12 ? 20 : void 0)} lengthAdjust="spacingAndGlyphs" class="z-20 col-1 row-1 mx-auto w-full self-end justify-self-start fill-white p-14 text-start font-momo-trust-display text-[0.25em] text-white">${ssrInterpolate(__props.title)}</text></svg><h1 class="sr-only">${ssrInterpolate(__props.title)}</h1></div>`);
    };
  }
});
const _sfc_setup$4 = _sfc_main$4.setup;
_sfc_main$4.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/Banner.vue");
  return _sfc_setup$4 ? _sfc_setup$4(props, ctx) : void 0;
};
const __nuxt_component_0 = Object.assign(_sfc_main$4, { __name: "Banner" });
function useHashScroll() {
  function scrollToHash(event, hashOrId) {
    const id = normalizeHashId(hashOrId);
    if (!id) return false;
    const target = (void 0).getElementById(id);
    if (!target) return false;
    event.preventDefault();
    const oldUrl = (void 0).location.href;
    const hash = `#${encodeURIComponent(id)}`;
    const newUrl = `${(void 0).location.pathname}${(void 0).location.search}${hash}`;
    (void 0).history.pushState(null, "", newUrl);
    (void 0).dispatchEvent(
      new CustomEvent("wiki:hash-scroll", { detail: { id } })
    );
    scrollToTarget(target);
    (void 0).dispatchEvent(
      new HashChangeEvent("hashchange", {
        oldURL: oldUrl,
        newURL: (void 0).location.href
      })
    );
    return true;
  }
  return { scrollToHash };
}
function normalizeHashId(hashOrId) {
  if (!hashOrId) return void 0;
  const value = hashOrId.trim();
  if (!value) return void 0;
  if (value.startsWith("#")) {
    return decodeHash(value.slice(1));
  }
  try {
    const url = new URL(value, (void 0).location.href);
    if (url.origin === (void 0).location.origin && url.pathname === (void 0).location.pathname && url.search === (void 0).location.search && url.hash) {
      return decodeHash(url.hash.slice(1));
    }
  } catch {
    return value;
  }
  return value.includes("#") ? void 0 : value;
}
function decodeHash(value) {
  try {
    return decodeURIComponent(value);
  } catch {
    return value;
  }
}
function scrollToTarget(target) {
  const scrollPaddingTop = Number.parseFloat(
    getComputedStyle((void 0).documentElement).scrollPaddingTop
  ) || 0;
  const scrollMarginTop = Number.parseFloat(getComputedStyle(target).scrollMarginTop) || 0;
  const top = target.getBoundingClientRect().top + (void 0).scrollY - scrollPaddingTop - scrollMarginTop;
  (void 0).scrollTo({ top, behavior: "smooth" });
}
const h2Style = "group m-2 flex w-[calc(100%-1rem)] items-stretch focus-visible:ring-2 focus-visible:ring-white focus-visible:outline-none md:text-lg lg:text-xl xl:text-2xl";
const h2DecorationStyle = "w-1 shrink-0 transition-[margin,width,background-color] duration-300 ease-out";
const activeH2DecorationStyle = "-ml-2 w-1 bg-primary-deep";
const h2TextStyle = "flex flex-1 items-center justify-start rounded-2xl px-4 py-2 text-left transition-[margin,border-radius,color,background-color] duration-300 ease-out";
const activeH2TextStyle = "-mr-2 rounded-none bg-secondary text-primary-dark";
const h3Style = "block pl-4 text-left font-belanosima hover:text-secondary md:text-md lg:text-lg xl:text-xl";
const activeH3Style = "text-secondary underline decoration-2 decoration-secondary underline-offset-4";
const scrollSpyActivationOffset = 8;
const hashScrollLockDuration = 2400;
const contentBarTransitionDuration = 300;
const contentId = "content-bar-toc";
const expandedContentBarClass = "w-1/6 pt-18 pb-10";
const collapsedContentBarClass = "h-24 w-18 pt-6 pb-6";
const _sfc_main$3 = /* @__PURE__ */ defineComponent({
  __name: "ContentBar",
  __ssrInlineRender: true,
  props: {
    toc: {}
  },
  setup(__props) {
    const props = __props;
    const { scrollToHash } = useHashScroll();
    const collapsed = ref(false);
    const contentTextRendered = ref(!collapsed.value);
    const contentTextVisible = ref(!collapsed.value);
    const expandedItems = ref([]);
    const activeH2Id = ref();
    const activeH3Id = ref();
    const contentScroll = ref(null);
    const canScrollUp = ref(false);
    const canScrollDown = ref(false);
    let hashScrollUntil = 0;
    let stopScrollSpy;
    let contentTextRevealTimer;
    let syncingExpandedItems = false;
    let preserveUserExpandedItemsAfterHashScroll = false;
    const contentBarClass = computed(() => [
      "sticky top-34 mb-6 max-h-[calc(100vh-11rem)] flex-col overflow-hidden rounded-r-[3.5em] bg-primary-norm  font-momo-trust-display shadow-lg transition-[width,height,padding,translate] duration-300 ease-out",
      collapsed.value ? collapsedContentBarClass : expandedContentBarClass,
      collapsed.value ? "-translate-x-6 hover:translate-x-0" : "translate-x-0"
    ]);
    const collapseButtonClass = computed(() => [
      "absolute top-4 right-4 z-20 flex h-16 w-14 items-center justify-center rounded-full text-secondary transition-colors duration-300 hover:text-primary-light focus-visible:ring-2 focus-visible:ring-white focus-visible:outline-none"
    ]);
    const contentTextClass = computed(() => [
      "transition-opacity duration-200 ease-out",
      contentTextVisible.value ? "opacity-100" : "opacity-0"
    ]);
    function clearContentTextRevealTimer() {
      if (contentTextRevealTimer) {
        clearTimeout(contentTextRevealTimer);
        contentTextRevealTimer = void 0;
      }
    }
    function hideContentText() {
      clearContentTextRevealTimer();
      contentTextVisible.value = false;
      contentTextRendered.value = false;
    }
    function revealContentTextAfterResize() {
      clearContentTextRevealTimer();
      contentTextVisible.value = false;
      contentTextRendered.value = false;
      contentTextRevealTimer = setTimeout(async () => {
        contentTextRendered.value = true;
        await nextTick();
        requestAnimationFrame(() => {
          contentTextVisible.value = true;
          updateScrollGradients();
        });
      }, contentBarTransitionDuration);
    }
    function updateScrollGradients() {
      const el = contentScroll.value;
      if (!el) return;
      const scrollTop = el.scrollTop;
      const clientHeight = el.clientHeight;
      const scrollHeight = el.scrollHeight;
      canScrollUp.value = scrollTop > 0;
      canScrollDown.value = scrollTop + clientHeight < scrollHeight - 1;
    }
    function h2Class() {
      return h2Style;
    }
    function h2DecorationClass(id) {
      return [
        h2DecorationStyle,
        activeH2Id.value === id && activeH2DecorationStyle
      ];
    }
    function h2TextClass(id) {
      return [
        h2TextStyle,
        activeH2Id.value === id ? activeH2TextStyle : "group-hover:bg-primary-light group-hover:text-primary-dark"
      ];
    }
    function h3Class(id) {
      return [h3Style, activeH3Id.value === id && activeH3Style];
    }
    async function keepActiveH3InView(id) {
      if (collapsed.value) return;
      await nextTick();
      const scroller = contentScroll.value;
      const link = findContentBarH3Link(id);
      if (!scroller || !link) return;
      const scrollerRect = scroller.getBoundingClientRect();
      const linkRect = link.getBoundingClientRect();
      const linkCenter = linkRect.top + linkRect.height / 2;
      const scrollerCenter = scrollerRect.top + scrollerRect.height / 2;
      scroller.scrollBy({
        top: linkCenter - scrollerCenter,
        behavior: "smooth"
      });
    }
    function findContentBarH3Link(id) {
      const escapedId = typeof CSS !== "undefined" && CSS.escape ? CSS.escape(id) : id;
      return contentScroll.value?.querySelector(
        `[data-content-bar-h3-id="${escapedId}"]`
      );
    }
    function collectArticleHeadings() {
      return [
        ...(void 0).querySelectorAll("main h2[id], main h3[id]")
      ];
    }
    function parentH2For(id) {
      if (props.toc.some((link) => link.id === id)) {
        return id;
      }
      return props.toc.find(
        (link) => link.children?.some((child) => child.id === id)
      )?.id;
    }
    async function syncExpandedItems(ids) {
      syncingExpandedItems = true;
      expandedItems.value = ids;
      await nextTick();
      syncingExpandedItems = false;
    }
    function setActiveHeading(id, isH3, shouldSyncExpandedItems = true) {
      const currentH2Id = parentH2For(id);
      activeH2Id.value = currentH2Id;
      activeH3Id.value = isH3 ? id : void 0;
      if (shouldSyncExpandedItems && currentH2Id && (expandedItems.value[0] !== currentH2Id || expandedItems.value.length > 1)) {
        void syncExpandedItems([currentH2Id]);
      }
    }
    function updateActiveHeading() {
      if (Date.now() < hashScrollUntil) return;
      const headings = collectArticleHeadings();
      if (!headings.length) return;
      const scrollLine = (void 0).scrollY + getScrollPaddingTop() + scrollSpyActivationOffset;
      let current = headings[0];
      for (const heading of headings) {
        if (headingStartOffset(heading) <= scrollLine) {
          current = heading;
        }
      }
      setActiveHeading(
        current.id,
        current.tagName.toLowerCase() === "h3",
        !preserveUserExpandedItemsAfterHashScroll
      );
      preserveUserExpandedItemsAfterHashScroll = false;
    }
    function lockToHashScrollDestination(id) {
      const target = (void 0).getElementById(id);
      const isH3 = target?.tagName.toLowerCase() === "h3";
      hashScrollUntil = Date.now() + hashScrollLockDuration;
      preserveUserExpandedItemsAfterHashScroll = false;
      setActiveHeading(id, isH3);
    }
    function getScrollPaddingTop() {
      const value = getComputedStyle((void 0).documentElement).scrollPaddingTop;
      return Number.parseFloat(value) || 0;
    }
    function headingStartOffset(heading) {
      const scrollMarginTop = Number.parseFloat(getComputedStyle(heading).scrollMarginTop) || 0;
      return heading.getBoundingClientRect().top + (void 0).scrollY - scrollMarginTop;
    }
    function setupScrollSpy() {
      stopScrollSpy?.();
      const update = () => updateActiveHeading();
      const updateAfterAnchorScroll = () => {
        (void 0).setTimeout(update, hashScrollLockDuration + 50);
      };
      const updateForHashScroll = (event) => {
        const { id } = event.detail ?? {};
        if (id) {
          lockToHashScrollDestination(id);
        }
      };
      (void 0).addEventListener("scroll", update, { passive: true });
      (void 0).addEventListener("resize", update);
      (void 0).addEventListener("hashchange", updateAfterAnchorScroll);
      (void 0).addEventListener("wiki:hash-scroll", updateForHashScroll);
      stopScrollSpy = () => {
        (void 0).removeEventListener("scroll", update);
        (void 0).removeEventListener("resize", update);
        (void 0).removeEventListener("hashchange", updateAfterAnchorScroll);
        (void 0).removeEventListener("wiki:hash-scroll", updateForHashScroll);
      };
      update();
    }
    watch(contentScroll, (newVal) => {
      if (newVal) {
        updateScrollGradients();
      }
    });
    watch(collapsed, (isCollapsed) => {
      if (isCollapsed) {
        hideContentText();
      } else {
        revealContentTextAfterResize();
      }
    });
    watch(expandedItems, () => {
      if (!syncingExpandedItems && Date.now() < hashScrollUntil) {
        preserveUserExpandedItemsAfterHashScroll = true;
      }
    });
    watch(activeH3Id, async (id) => {
      if (id) {
        await keepActiveH3InView(id);
      }
    });
    watch(
      () => props.toc,
      async () => {
        await nextTick();
        setupScrollSpy();
      },
      { deep: true }
    );
    return (_ctx, _push, _parent, _attrs) => {
      _push(`<nav${ssrRenderAttrs(mergeProps({
        class: unref(contentBarClass),
        "data-content-bar-collapsed": unref(collapsed),
        "aria-labelledby": "toc-title"
      }, _attrs))}><h2 class="sr-only" id="toc-title">Table of contents</h2>`);
      if (unref(contentTextRendered)) {
        _push(`<div class="${ssrRenderClass([unref(contentTextClass), "absolute top-4 right-18 left-0 z-10 flex h-16 items-center justify-center text-primary-deep lg:text-xl xl:text-3xl"])}" aria-hidden="true"> Contents </div>`);
      } else {
        _push(`<!---->`);
      }
      _push(`<button type="button" class="${ssrRenderClass(unref(collapseButtonClass))}"${ssrRenderAttr("aria-controls", contentId)}${ssrRenderAttr("aria-expanded", !unref(collapsed))}${ssrRenderAttr(
        "aria-label",
        unref(collapsed) ? "Expand table of contents" : "Collapse table of contents"
      )}><svg xmlns="http://www.w3.org/2000/svg" viewBox="-6 -7 18 14" class="${ssrRenderClass([unref(collapsed) && "rotate-180", "h-16 w-24 transition-transform duration-300"])}" aria-hidden="true"><path d="M9 6 0 0 9-6Z" fill="currentColor"></path><path d="M-6 0 5-7 5.393-6.024-4 0 5.479 6.01 5 7-6 0Z" fill="currentColor"></path></svg></button>`);
      if (unref(contentTextRendered)) {
        _push(`<div${ssrRenderAttr("id", contentId)} class="${ssrRenderClass([unref(contentTextClass), "max-h-full min-h-0 w-full flex-1 scrollbar-none overflow-y-auto"])}"${ssrRenderAttr("aria-hidden", !unref(contentTextVisible))}>`);
        _push(ssrRenderComponent(unref(AccordionRoot), {
          modelValue: unref(expandedItems),
          "onUpdate:modelValue": ($event) => isRef(expandedItems) ? expandedItems.value = $event : null,
          type: "multiple",
          class: "w-full"
        }, {
          default: withCtx((_, _push2, _parent2, _scopeId) => {
            if (_push2) {
              _push2(`<!--[-->`);
              ssrRenderList(__props.toc, (child) => {
                _push2(`<!--[-->`);
                if (child.children?.length) {
                  _push2(ssrRenderComponent(unref(AccordionItem), {
                    value: child.id,
                    class: "overflow-hidden text-white"
                  }, {
                    default: withCtx((_2, _push3, _parent3, _scopeId2) => {
                      if (_push3) {
                        _push3(ssrRenderComponent(unref(AccordionHeader), { class: "flex" }, {
                          default: withCtx((_3, _push4, _parent4, _scopeId3) => {
                            if (_push4) {
                              _push4(ssrRenderComponent(unref(AccordionTrigger), {
                                class: h2Class()
                              }, {
                                default: withCtx((_4, _push5, _parent5, _scopeId4) => {
                                  if (_push5) {
                                    _push5(`<span class="${ssrRenderClass(h2DecorationClass(child.id))}" aria-hidden="true"${_scopeId4}></span><span class="${ssrRenderClass(h2TextClass(child.id))}"${_scopeId4}>${ssrInterpolate(child.text)}</span>`);
                                  } else {
                                    return [
                                      createVNode("span", {
                                        class: h2DecorationClass(child.id),
                                        "aria-hidden": "true"
                                      }, null, 2),
                                      createVNode("span", {
                                        class: h2TextClass(child.id)
                                      }, toDisplayString(child.text), 3)
                                    ];
                                  }
                                }),
                                _: 2
                              }, _parent4, _scopeId3));
                            } else {
                              return [
                                createVNode(unref(AccordionTrigger), {
                                  class: h2Class()
                                }, {
                                  default: withCtx(() => [
                                    createVNode("span", {
                                      class: h2DecorationClass(child.id),
                                      "aria-hidden": "true"
                                    }, null, 2),
                                    createVNode("span", {
                                      class: h2TextClass(child.id)
                                    }, toDisplayString(child.text), 3)
                                  ]),
                                  _: 2
                                }, 1032, ["class"])
                              ];
                            }
                          }),
                          _: 2
                        }, _parent3, _scopeId2));
                        _push3(ssrRenderComponent(unref(AccordionContent), { class: "content-bar-accordion-content overflow-hidden data-[state=closed]:animate-[content-bar-slide-up_500ms_ease-in] data-[state=open]:animate-[content-bar-slide-down_500ms_ease-out]" }, {
                          default: withCtx((_3, _push4, _parent4, _scopeId3) => {
                            if (_push4) {
                              _push4(`<ul class="ml-8 space-y-2"${_scopeId3}><!--[-->`);
                              ssrRenderList(child.children, (value) => {
                                _push4(`<li class="flex justify-center"${_scopeId3}><div class="flex w-4/5 -translate-x-4 items-center"${_scopeId3}><span class="h-2 w-2 shrink-0 rounded-full bg-white" aria-hidden="true"${_scopeId3}></span><a${ssrRenderAttr("href", `#${value.id}`)} class="${ssrRenderClass(h3Class(value.id))}"${ssrRenderAttr("data-content-bar-h3-id", value.id)}${_scopeId3}>${ssrInterpolate(value.text)}</a></div></li>`);
                              });
                              _push4(`<!--]--></ul>`);
                            } else {
                              return [
                                createVNode("ul", { class: "ml-8 space-y-2" }, [
                                  (openBlock(true), createBlock(Fragment, null, renderList(child.children, (value) => {
                                    return openBlock(), createBlock("li", {
                                      key: value.id,
                                      class: "flex justify-center"
                                    }, [
                                      createVNode("div", { class: "flex w-4/5 -translate-x-4 items-center" }, [
                                        createVNode("span", {
                                          class: "h-2 w-2 shrink-0 rounded-full bg-white",
                                          "aria-hidden": "true"
                                        }),
                                        createVNode("a", {
                                          href: `#${value.id}`,
                                          class: h3Class(value.id),
                                          "data-content-bar-h3-id": value.id,
                                          onClick: ($event) => unref(scrollToHash)($event, value.id)
                                        }, toDisplayString(value.text), 11, ["href", "data-content-bar-h3-id", "onClick"])
                                      ])
                                    ]);
                                  }), 128))
                                ])
                              ];
                            }
                          }),
                          _: 2
                        }, _parent3, _scopeId2));
                      } else {
                        return [
                          createVNode(unref(AccordionHeader), { class: "flex" }, {
                            default: withCtx(() => [
                              createVNode(unref(AccordionTrigger), {
                                class: h2Class()
                              }, {
                                default: withCtx(() => [
                                  createVNode("span", {
                                    class: h2DecorationClass(child.id),
                                    "aria-hidden": "true"
                                  }, null, 2),
                                  createVNode("span", {
                                    class: h2TextClass(child.id)
                                  }, toDisplayString(child.text), 3)
                                ]),
                                _: 2
                              }, 1032, ["class"])
                            ]),
                            _: 2
                          }, 1024),
                          createVNode(unref(AccordionContent), { class: "content-bar-accordion-content overflow-hidden data-[state=closed]:animate-[content-bar-slide-up_500ms_ease-in] data-[state=open]:animate-[content-bar-slide-down_500ms_ease-out]" }, {
                            default: withCtx(() => [
                              createVNode("ul", { class: "ml-8 space-y-2" }, [
                                (openBlock(true), createBlock(Fragment, null, renderList(child.children, (value) => {
                                  return openBlock(), createBlock("li", {
                                    key: value.id,
                                    class: "flex justify-center"
                                  }, [
                                    createVNode("div", { class: "flex w-4/5 -translate-x-4 items-center" }, [
                                      createVNode("span", {
                                        class: "h-2 w-2 shrink-0 rounded-full bg-white",
                                        "aria-hidden": "true"
                                      }),
                                      createVNode("a", {
                                        href: `#${value.id}`,
                                        class: h3Class(value.id),
                                        "data-content-bar-h3-id": value.id,
                                        onClick: ($event) => unref(scrollToHash)($event, value.id)
                                      }, toDisplayString(value.text), 11, ["href", "data-content-bar-h3-id", "onClick"])
                                    ])
                                  ]);
                                }), 128))
                              ])
                            ]),
                            _: 2
                          }, 1024)
                        ];
                      }
                    }),
                    _: 2
                  }, _parent2, _scopeId));
                } else {
                  _push2(`<a${ssrRenderAttr("href", `#${child.id}`)} class="${ssrRenderClass(h2Class())}"${_scopeId}><span class="${ssrRenderClass(h2DecorationClass(child.id))}" aria-hidden="true"${_scopeId}></span><span class="${ssrRenderClass(h2TextClass(child.id))}"${_scopeId}>${ssrInterpolate(child.text)}</span></a>`);
                }
                _push2(`<!--]-->`);
              });
              _push2(`<!--]-->`);
            } else {
              return [
                (openBlock(true), createBlock(Fragment, null, renderList(__props.toc, (child) => {
                  return openBlock(), createBlock(Fragment, {
                    key: child.id
                  }, [
                    child.children?.length ? (openBlock(), createBlock(unref(AccordionItem), {
                      key: 0,
                      value: child.id,
                      class: "overflow-hidden text-white"
                    }, {
                      default: withCtx(() => [
                        createVNode(unref(AccordionHeader), { class: "flex" }, {
                          default: withCtx(() => [
                            createVNode(unref(AccordionTrigger), {
                              class: h2Class()
                            }, {
                              default: withCtx(() => [
                                createVNode("span", {
                                  class: h2DecorationClass(child.id),
                                  "aria-hidden": "true"
                                }, null, 2),
                                createVNode("span", {
                                  class: h2TextClass(child.id)
                                }, toDisplayString(child.text), 3)
                              ]),
                              _: 2
                            }, 1032, ["class"])
                          ]),
                          _: 2
                        }, 1024),
                        createVNode(unref(AccordionContent), { class: "content-bar-accordion-content overflow-hidden data-[state=closed]:animate-[content-bar-slide-up_500ms_ease-in] data-[state=open]:animate-[content-bar-slide-down_500ms_ease-out]" }, {
                          default: withCtx(() => [
                            createVNode("ul", { class: "ml-8 space-y-2" }, [
                              (openBlock(true), createBlock(Fragment, null, renderList(child.children, (value) => {
                                return openBlock(), createBlock("li", {
                                  key: value.id,
                                  class: "flex justify-center"
                                }, [
                                  createVNode("div", { class: "flex w-4/5 -translate-x-4 items-center" }, [
                                    createVNode("span", {
                                      class: "h-2 w-2 shrink-0 rounded-full bg-white",
                                      "aria-hidden": "true"
                                    }),
                                    createVNode("a", {
                                      href: `#${value.id}`,
                                      class: h3Class(value.id),
                                      "data-content-bar-h3-id": value.id,
                                      onClick: ($event) => unref(scrollToHash)($event, value.id)
                                    }, toDisplayString(value.text), 11, ["href", "data-content-bar-h3-id", "onClick"])
                                  ])
                                ]);
                              }), 128))
                            ])
                          ]),
                          _: 2
                        }, 1024)
                      ]),
                      _: 2
                    }, 1032, ["value"])) : (openBlock(), createBlock("a", {
                      key: 1,
                      href: `#${child.id}`,
                      class: h2Class(),
                      onClick: ($event) => unref(scrollToHash)($event, child.id)
                    }, [
                      createVNode("span", {
                        class: h2DecorationClass(child.id),
                        "aria-hidden": "true"
                      }, null, 2),
                      createVNode("span", {
                        class: h2TextClass(child.id)
                      }, toDisplayString(child.text), 3)
                    ], 10, ["href", "onClick"]))
                  ], 64);
                }), 128))
              ];
            }
          }),
          _: 1
        }, _parent));
        _push(`</div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(contentTextVisible) && unref(canScrollUp)) {
        _push(`<div class="pointer-events-none absolute top-18 z-10 h-8 w-full bg-linear-to-b from-primary-norm to-transparent"></div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(contentTextVisible) && unref(canScrollDown)) {
        _push(`<div class="pointer-events-none absolute bottom-10 z-10 h-8 w-full bg-linear-to-t from-primary-norm to-transparent"></div>`);
      } else {
        _push(`<!---->`);
      }
      _push(`</nav>`);
    };
  }
});
const _sfc_setup$3 = _sfc_main$3.setup;
_sfc_main$3.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/ContentBar.vue");
  return _sfc_setup$3 ? _sfc_setup$3(props, ctx) : void 0;
};
const __nuxt_component_1 = Object.assign(_sfc_main$3, { __name: "ContentBar" });
const htmlTags = /* @__PURE__ */ new Set([
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hgroup",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "math",
  "menu",
  "menuitem",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "param",
  "picture",
  "pre",
  "progress",
  "q",
  "rb",
  "rp",
  "rt",
  "rtc",
  "ruby",
  "s",
  "samp",
  "script",
  "section",
  "select",
  "slot",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "svg",
  "table",
  "tbody",
  "td",
  "template",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "track",
  "u",
  "ul",
  "var",
  "video",
  "wbr"
]);
const TEXT_TAGS = ["p", "h1", "h2", "h3", "h4", "h5", "h6", "li"];
function isTag(vnode, tag) {
  if (vnode.type === tag) {
    return true;
  }
  if (typeof vnode.type === "object" && vnode.type.tag === tag) {
    return true;
  }
  if (vnode.tag === tag) {
    return true;
  }
  return false;
}
function isText(vnode) {
  return isTag(vnode, "text") || isTag(vnode, /* @__PURE__ */ Symbol.for("v-txt"));
}
function nodeChildren(node) {
  if (Array.isArray(node.children) || typeof node.children === "string") {
    return node.children;
  }
  if (typeof node.children?.default === "function") {
    return node.children.default();
  }
  return [];
}
function nodeTextContent(node) {
  if (!node) {
    return "";
  }
  if (Array.isArray(node)) {
    return node.map(nodeTextContent).join("");
  }
  if (isText(node)) {
    return node.value || node.children || "";
  }
  const children = nodeChildren(node);
  if (Array.isArray(children)) {
    return children.map(nodeTextContent).filter(Boolean).join("");
  }
  return "";
}
function unwrap(vnode, tags = []) {
  if (Array.isArray(vnode)) {
    return vnode.flatMap((node) => unwrap(node, tags));
  }
  let result = vnode;
  if (tags.some((tag) => tag === "*" || isTag(vnode, tag))) {
    result = nodeChildren(vnode) || vnode;
    if (!Array.isArray(result) && TEXT_TAGS.some((tag) => isTag(vnode, tag))) {
      result = [result];
    }
  }
  return result;
}
function _flatUnwrap(vnodes, tags = []) {
  vnodes = Array.isArray(vnodes) ? vnodes : [vnodes];
  if (!tags.length) {
    return vnodes;
  }
  return vnodes.flatMap((vnode) => _flatUnwrap(unwrap(vnode, [tags[0]]), tags.slice(1))).filter((vnode) => !(isText(vnode) && nodeTextContent(vnode).trim() === ""));
}
function flatUnwrap(vnodes, tags = []) {
  if (typeof tags === "string") {
    tags = tags.split(/[,\s]/).map((tag) => tag.trim()).filter(Boolean);
  }
  if (!tags.length) {
    return vnodes;
  }
  return _flatUnwrap(vnodes, tags).reduce((acc, item) => {
    if (isText(item)) {
      if (typeof acc[acc.length - 1] === "string") {
        acc[acc.length - 1] += item.children;
      } else {
        acc.push(item.children);
      }
    } else {
      acc.push(item);
    }
    return acc;
  }, []);
}
function pick(obj, keys) {
  return keys.reduce((acc, key) => {
    const value = get(obj, key);
    if (value !== void 0) {
      acc[key] = value;
    }
    return acc;
  }, {});
}
function get(obj, key) {
  return key.split(".").reduce((acc, k) => acc && acc[k], obj);
}
const DEFAULT_SLOT = "default";
const rxOn = /^@|^v-on:/;
const rxBind = /^:|^v-bind:/;
const rxModel = /^v-model/;
const nativeInputs = ["select", "textarea", "input"];
const specialParentTags = /* @__PURE__ */ new Set(["math", "svg"]);
const customElements = /* @__PURE__ */ new Set();
const proseComponentMap = Object.fromEntries(["p", "a", "blockquote", "code", "pre", "code", "em", "h1", "h2", "h3", "h4", "h5", "h6", "hr", "img", "ul", "ol", "li", "strong", "table", "thead", "tbody", "td", "th", "tr", "script"].map((t) => [t, `prose-${t}`]));
const dangerousTags = ["script", "base"];
const _sfc_main$2 = defineComponent({
  name: "MDCRenderer",
  props: {
    /**
     * Content to render
     */
    body: {
      type: Object,
      required: true
    },
    /**
     * Document meta data
     */
    data: {
      type: Object,
      default: () => ({})
    },
    /**
     * Class(es) to bind to the component
     */
    class: {
      type: [String, Object],
      default: void 0
    },
    /**
     * Root tag to use for rendering
     */
    tag: {
      type: [String, Boolean],
      default: void 0
    },
    /**
     * Whether or not to render Prose components instead of HTML tags
     */
    prose: {
      type: Boolean,
      default: void 0
    },
    /**
     * The map of custom components to use for rendering.
     */
    components: {
      type: Object,
      default: () => ({})
    },
    /**
     * Tags to unwrap separated by spaces
     * Example: 'ul li'
     */
    unwrap: {
      type: [Boolean, String],
      default: false
    }
  },
  async setup(props) {
    const app = getCurrentInstance()?.appContext?.app;
    const $nuxt = app?.$nuxt;
    const route = $nuxt?.$route || $nuxt?._route;
    const { mdc } = $nuxt?.$config?.public || {};
    const customElementTags = mdc?.components?.customElements || mdc?.components?.custom;
    if (customElementTags) {
      customElementTags.forEach((tag) => customElements.add(tag));
    }
    const tags = computed(() => ({
      ...mdc?.components?.prose && props.prose !== false ? proseComponentMap : {},
      ...mdc?.components?.map || {},
      ...toRaw(props.data?.mdc?.components || {}),
      ...props.components
    }));
    const contentKey = computed(() => {
      const components = (props.body?.children || []).map((n) => n.tag || n.type).filter((t) => !ignoreTag(t));
      return Array.from(new Set(components)).sort().join(".");
    });
    const runtimeData = reactive({
      ...props.data
    });
    watch(() => props.data, (newData) => {
      Object.assign(runtimeData, newData);
    });
    await resolveContentComponents(props.body, { tags: tags.value });
    function updateRuntimeData(code, value) {
      const lastIndex = code.split(".").length - 1;
      return code.split(".").reduce((o, k, i) => {
        if (i == lastIndex && o) {
          o[k] = value;
          return o[k];
        }
        return typeof o === "object" ? o[k] : void 0;
      }, runtimeData);
    }
    return { tags, contentKey, route, runtimeData, updateRuntimeData };
  },
  render(ctx) {
    const { tags, tag, body, data, contentKey, route, unwrap: unwrap2, runtimeData, updateRuntimeData } = ctx;
    if (!body) {
      return null;
    }
    const meta = { ...data, tags, $route: route, runtimeData, updateRuntimeData };
    const component = tag !== false ? resolveComponentInstance(tag || meta.component?.name || meta.component || "div") : void 0;
    return component ? h(component, { ...meta.component?.props, class: ctx.class, ...this.$attrs, key: contentKey }, { default: defaultSlotRenderer }) : defaultSlotRenderer?.();
    function defaultSlotRenderer() {
      const defaultSlot = _renderSlots(body, h, { documentMeta: meta, parentScope: meta, resolveComponent: resolveComponentInstance });
      if (!defaultSlot?.default) {
        return null;
      }
      if (unwrap2) {
        return flatUnwrap(
          defaultSlot.default(),
          typeof unwrap2 === "string" ? unwrap2.split(" ") : ["*"]
        );
      }
      return defaultSlot.default();
    }
  }
});
function _renderNode(node, h2, options, keyInParent) {
  const { documentMeta, parentScope, resolveComponent: resolveComponent2 } = options;
  if (node.type === "text") {
    return h2(Text, node.value);
  }
  if (node.type === "comment") {
    return h2(Comment, null, node.value);
  }
  const originalTag = node.tag;
  const renderTag = findMappedTag(node, documentMeta.tags);
  if (node.tag === "binding") {
    return renderBinding(node, h2, documentMeta, parentScope);
  }
  const _resolveComponent = isUnresolvableTag(renderTag) ? (component2) => component2 : resolveComponent2;
  if (dangerousTags.includes(pascalCase(renderTag).toLowerCase())) {
    return h2(
      "pre",
      { class: "mdc-renderer-dangerous-tag" },
      "<" + renderTag + ">" + nodeTextContent(node) + "</" + renderTag + ">"
    );
  }
  const component = _resolveComponent(renderTag);
  if (typeof component === "object") {
    component.tag = originalTag;
  }
  const props = propsToData(node, documentMeta);
  if (keyInParent) {
    props.key = keyInParent;
  }
  return h2(
    component,
    props,
    _renderSlots(
      node,
      h2,
      {
        documentMeta,
        parentScope: { ...parentScope, ...props },
        resolveComponent: _resolveComponent
      }
    )
  );
}
function _renderSlots(node, h2, options) {
  const { documentMeta, parentScope, resolveComponent: resolveComponent2 } = options;
  const children = node.children || [];
  const slotNodes = children.reduce((data, node2) => {
    if (!isTemplate(node2)) {
      data[DEFAULT_SLOT].children.push(node2);
      return data;
    }
    const slotName = getSlotName(node2);
    data[slotName] = data[slotName] || { props: {}, children: [] };
    if (node2.type === "element") {
      data[slotName].props = node2.props;
      data[slotName].children.push(...node2.children || []);
    }
    return data;
  }, {
    [DEFAULT_SLOT]: { props: {}, children: [] }
  });
  const slots = Object.entries(slotNodes).reduce((slots2, [name, { props, children: children2 }]) => {
    if (!children2.length) {
      return slots2;
    }
    slots2[name] = (data = {}) => {
      const scopedProps = pick(data, Object.keys(props || {}));
      let vNodes = children2.map((child, index) => {
        return _renderNode(
          child,
          h2,
          {
            documentMeta,
            parentScope: { ...parentScope, ...scopedProps },
            resolveComponent: resolveComponent2
          },
          String(child.props?.key || index)
        );
      });
      if (props?.unwrap) {
        vNodes = flatUnwrap(vNodes, props.unwrap);
      }
      return mergeTextNodes(vNodes);
    };
    return slots2;
  }, {});
  return slots;
}
function renderBinding(node, h2, documentMeta, parentScope = {}) {
  const data = {
    ...documentMeta.runtimeData,
    ...parentScope,
    $document: documentMeta,
    $doc: documentMeta
  };
  const splitter = /\.|\[(\d+)\]/;
  const keys = node.props?.value.trim().split(splitter).filter(Boolean);
  const value = keys.reduce((data2, key) => {
    if (data2 && key in data2) {
      if (typeof data2[key] === "function") {
        return data2[key]();
      } else {
        return data2[key];
      }
    }
    return void 0;
  }, data);
  const defaultValue = node.props?.defaultValue;
  return h2(Text, value ?? defaultValue ?? "");
}
function propsToData(node, documentMeta) {
  const { tag = "", props = {} } = node;
  return Object.keys(props).reduce(function(data, key) {
    if (key === "__ignoreMap") {
      return data;
    }
    const value = props[key];
    if (rxModel.test(key)) {
      return propsToDataRxModel(key, value, data, documentMeta, { native: nativeInputs.includes(tag) });
    }
    if (key === "v-bind") {
      return propsToDataVBind(key, value, data, documentMeta);
    }
    if (rxOn.test(key)) {
      return propsToDataRxOn(key, value, data, documentMeta);
    }
    if (rxBind.test(key)) {
      return propsToDataRxBind(key, value, data, documentMeta);
    }
    const { attribute } = find(html, key);
    if (Array.isArray(value) && value.every((v) => typeof v === "string")) {
      data[attribute] = value.join(" ");
      return data;
    }
    data[attribute] = value;
    return data;
  }, {});
}
function propsToDataRxModel(key, value, data, documentMeta, { native }) {
  const propName = key.match(/^v-model:([^=]+)/)?.[1] || "modelValue";
  const field = native ? "value" : propName;
  const event = native ? "onInput" : `onUpdate:${propName}`;
  data[field] = evalInContext(value, documentMeta.runtimeData);
  data[event] = (e) => {
    documentMeta.updateRuntimeData(value, native ? e.target?.value : e);
  };
  return data;
}
function propsToDataVBind(_key, value, data, documentMeta) {
  const val = evalInContext(value, documentMeta);
  data = Object.assign(data, val);
  return data;
}
function propsToDataRxOn(key, value, data, documentMeta) {
  key = key.replace(rxOn, "");
  data.on = data.on || {};
  data.on[key] = () => evalInContext(value, documentMeta);
  return data;
}
function propsToDataRxBind(key, value, data, documentMeta) {
  key = key.replace(rxBind, "");
  data[key] = evalInContext(value, documentMeta);
  return data;
}
const resolveComponentInstance = (component) => {
  if (typeof component === "string") {
    if (ignoreTag(component)) {
      return component;
    }
    const _component = resolveComponent(pascalCase(component), false);
    if (!component || _component?.name === "AsyncComponentWrapper") {
      return _component;
    }
    if (typeof _component === "string") {
      return _component;
    }
    if ("setup" in _component) {
      return defineAsyncComponent(() => new Promise((resolve) => resolve(_component)));
    }
    return _component;
  }
  return component;
};
function evalInContext(code, context) {
  const result = code.split(".").reduce((o, k) => typeof o === "object" ? o[k] : void 0, context);
  return typeof result === "undefined" ? destr(code) : result;
}
function getSlotName(node) {
  let name = "";
  for (const propName of Object.keys(node.props || {})) {
    if (!propName.startsWith("#") && !propName.startsWith("v-slot:")) {
      continue;
    }
    name = propName.split(/[:#]/, 2)[1];
    break;
  }
  return name || DEFAULT_SLOT;
}
function isTemplate(node) {
  return node.tag === "template";
}
function isUnresolvableTag(tag) {
  return specialParentTags.has(tag);
}
function mergeTextNodes(nodes) {
  const mergedNodes = [];
  for (const node of nodes) {
    const previousNode = mergedNodes[mergedNodes.length - 1];
    if (node.type === Text && previousNode?.type === Text) {
      previousNode.children = previousNode.children + node.children;
    } else {
      mergedNodes.push(node);
    }
  }
  return mergedNodes;
}
async function resolveContentComponents(body, meta) {
  if (!body) {
    return;
  }
  const components = Array.from(new Set(loadComponents(body, meta)));
  await Promise.all(components.map(async (c) => {
    if (c?.render || c?.ssrRender || c?.__ssrInlineRender) {
      return;
    }
    const resolvedComponent = resolveComponentInstance(c);
    if (resolvedComponent?.__asyncLoader && !resolvedComponent.__asyncResolved) {
      await resolvedComponent.__asyncLoader();
    }
  }));
  function loadComponents(node, documentMeta) {
    const tag = node.tag;
    if (node.type === "text" || tag === "binding" || node.type === "comment") {
      return [];
    }
    const renderTag = findMappedTag(node, documentMeta.tags);
    if (isUnresolvableTag(renderTag)) {
      return [];
    }
    const components2 = [];
    if (node.type !== "root" && !ignoreTag(renderTag)) {
      components2.push(renderTag);
    }
    for (const child of node.children || []) {
      components2.push(...loadComponents(child, documentMeta));
    }
    return components2;
  }
}
function findMappedTag(node, tags) {
  const tag = node.tag;
  if (!tag || typeof node.props?.__ignoreMap !== "undefined") {
    return tag;
  }
  return tags[tag] || tags[pascalCase(tag)] || tags[kebabCase(node.tag)] || tag;
}
function ignoreTag(tag) {
  const isCustomEl = typeof tag === "string" ? customElements.has(tag) : false;
  return isCustomEl || htmlTags.has(tag);
}
const _sfc_setup$2 = _sfc_main$2.setup;
_sfc_main$2.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("../node_modules/@nuxtjs/mdc/dist/runtime/components/MDCRenderer.vue");
  return _sfc_setup$2 ? _sfc_setup$2(props, ctx) : void 0;
};
const MDCRenderer = Object.assign(_sfc_main$2, { __name: "MDCRenderer" });
const pickExport = (mod, exportName, componentName, path) => {
  const resolved = exportName === "default" ? mod?.default : mod?.[exportName];
  if (!resolved) {
    throw new Error(`[nuxt-content] Missing export "${exportName}" for component "${componentName}" in "${path}".`);
  }
  return resolved;
};
const localComponentLoaders = {
  ProseA: () => import("./ProseA-ClSFSp8T.js").then((m) => pickExport(m, "default", "ProseA", "./../../../../../app/components/content/ProseA.vue")),
  ProseBlockquote: () => import("./ProseBlockquote-DOMeRiAy.js").then((m) => pickExport(m, "default", "ProseBlockquote", "./../../../../../app/components/content/ProseBlockquote.vue")),
  ProseCode: () => import("./ProseCode-DpjZcynR.js").then((m) => pickExport(m, "default", "ProseCode", "./../../../../../app/components/content/ProseCode.vue")),
  ProseEm: () => import("./ProseEm-CQAjNVZW.js").then((m) => pickExport(m, "default", "ProseEm", "./../../../../../app/components/content/ProseEm.vue")),
  ProseH2: () => import("./ProseH2-D-cViqTv.js").then((m) => pickExport(m, "default", "ProseH2", "./../../../../../app/components/content/ProseH2.vue")),
  ProseH3: () => import("./ProseH3-BqPuVRHC.js").then((m) => pickExport(m, "default", "ProseH3", "./../../../../../app/components/content/ProseH3.vue")),
  ProseLi: () => import("./ProseLi-BjuWeRoP.js").then((m) => pickExport(m, "default", "ProseLi", "./../../../../../app/components/content/ProseLi.vue")),
  ProseOl: () => import("./ProseOl-BIQ5_RpU.js").then((m) => pickExport(m, "default", "ProseOl", "./../../../../../app/components/content/ProseOl.vue")),
  ProseP: () => import("./ProseP-DdMnJkLf.js").then((m) => pickExport(m, "default", "ProseP", "./../../../../../app/components/content/ProseP.vue")),
  ProsePre: () => import("./ProsePre-DygdOLUP.js").then((m) => pickExport(m, "default", "ProsePre", "./../../../../../app/components/content/ProsePre.vue")),
  ProseStrong: () => import("./ProseStrong-BngaV-gu.js").then((m) => pickExport(m, "default", "ProseStrong", "./../../../../../app/components/content/ProseStrong.vue")),
  ProseTable: () => import("./ProseTable-BGRsJHs4.js").then((m) => pickExport(m, "default", "ProseTable", "./../../../../../app/components/content/ProseTable.vue")),
  ProseTbody: () => import("./ProseTbody-lM8-Pv3j.js").then((m) => pickExport(m, "default", "ProseTbody", "./../../../../../app/components/content/ProseTbody.vue")),
  ProseTd: () => import("./ProseTd-LjGO9H3r.js").then((m) => pickExport(m, "default", "ProseTd", "./../../../../../app/components/content/ProseTd.vue")),
  ProseTh: () => import("./ProseTh-BP4MgdYu.js").then((m) => pickExport(m, "default", "ProseTh", "./../../../../../app/components/content/ProseTh.vue")),
  ProseThead: () => import("./ProseThead-CHe4Kotg.js").then((m) => pickExport(m, "default", "ProseThead", "./../../../../../app/components/content/ProseThead.vue")),
  ProseTr: () => import("./ProseTr-DWfCUirl.js").then((m) => pickExport(m, "default", "ProseTr", "./../../../../../app/components/content/ProseTr.vue")),
  ProseUl: () => import("./ProseUl-D5QvUmaD.js").then((m) => pickExport(m, "default", "ProseUl", "./../../../../../app/components/content/ProseUl.vue")),
  RefFn: () => import("./RefFn-DeS-tSOG.js").then((m) => pickExport(m, "default", "RefFn", "./../../../../../app/components/RefFn.vue")),
  Reference: () => import("./Reference-DWjCWjLR.js").then((m) => pickExport(m, "default", "Reference", "./../../../../../app/components/Reference.vue"))
};
const globalComponents = [];
const localComponents = ["ProseA", "ProseBlockquote", "ProseCode", "ProseEm", "ProseH2", "ProseH3", "ProseLi", "ProseOl", "ProseP", "ProsePre", "ProseStrong", "ProseTable", "ProseTbody", "ProseTd", "ProseTh", "ProseThead", "ProseTr", "ProseUl", "RefFn", "Reference"];
const _sfc_main$1 = {
  __name: "ContentRenderer",
  __ssrInlineRender: true,
  props: {
    /**
     * Content to render
     */
    value: {
      type: Object,
      required: true
    },
    /**
     * Render only the excerpt
     */
    excerpt: {
      type: Boolean,
      default: false
    },
    /**
     * Root tag to use for rendering
     */
    tag: {
      type: String,
      default: "div"
    },
    /**
     * The map of custom components to use for rendering.
     */
    components: {
      type: Object,
      default: () => ({})
    },
    data: {
      type: Object,
      default: () => ({})
    },
    /**
     * Whether or not to render Prose components instead of HTML tags
     */
    prose: {
      type: Boolean,
      default: void 0
    },
    /**
     * Root tag to use for rendering
     */
    class: {
      type: [String, Object],
      default: void 0
    },
    /**
     * Tags to unwrap separated by spaces
     * Example: 'ul li'
     */
    unwrap: {
      type: [Boolean, String],
      default: false
    }
  },
  setup(__props) {
    const renderFunctions = ["render", "ssrRender", "__ssrInlineRender"];
    const props = __props;
    const debug = import.meta.preview;
    const body = computed(() => {
      let body2 = props.value.body || props.value;
      if (props.excerpt && props.value.excerpt) {
        body2 = props.value.excerpt;
      }
      if (body2.type === "minimal" || body2.type === "minimark") {
        return toHast({ type: "minimark", value: body2.value });
      }
      return body2;
    });
    const isEmpty = computed(() => !body.value?.children?.length);
    const data = computed(() => {
      const { body: body2, excerpt, ...data2 } = props.value;
      return {
        ...data2,
        ...props.data
      };
    });
    const proseComponentMap2 = Object.fromEntries(["p", "a", "blockquote", "code", "pre", "code", "em", "h1", "h2", "h3", "h4", "h5", "h6", "hr", "img", "ul", "ol", "li", "strong", "table", "thead", "tbody", "td", "th", "tr", "script"].map((t) => [t, `prose-${t}`]));
    const { mdc } = useRuntimeConfig().public || {};
    const propsDataMDC = computed(() => props.data.mdc);
    const tags = computed(() => ({
      ...mdc?.components?.prose && props.prose !== false ? proseComponentMap2 : {},
      ...mdc?.components?.map || {},
      ...toRaw(propsDataMDC.value?.components || {}),
      ...props.components
    }));
    const componentsMap = computed(() => {
      return body.value ? resolveContentComponents2(body.value, { tags: tags.value }) : {};
    });
    function resolveVueComponent(component) {
      let _component = component;
      if (typeof component === "string") {
        if (htmlTags.has(component)) {
          return component;
        }
        if (globalComponents.includes(pascalCase(component))) {
          _component = resolveComponent(component, false);
        } else if (localComponents.includes(pascalCase(component))) {
          const loader = localComponentLoaders[pascalCase(component)];
          _component = loader ? defineAsyncComponent(loader) : void 0;
        }
        if (typeof _component === "string") {
          return _component;
        }
      }
      if (!_component) {
        return _component;
      }
      const componentObject = _component;
      if ("__asyncLoader" in componentObject) {
        return componentObject;
      }
      if ("setup" in componentObject) {
        return defineAsyncComponent(() => Promise.resolve(componentObject));
      }
      return componentObject;
    }
    function resolveContentComponents2(body2, meta) {
      if (!body2) {
        return;
      }
      const components = Array.from(new Set(loadComponents(body2, meta)));
      const result = {};
      for (const [tag, component] of components) {
        if (result[tag]) {
          continue;
        }
        if (typeof component === "object" && renderFunctions.some((fn) => Object.hasOwnProperty.call(component, fn))) {
          result[tag] = component;
          continue;
        }
        result[tag] = resolveVueComponent(component);
      }
      return result;
    }
    function loadComponents(node, documentMeta) {
      const tag = node.tag;
      if (node.type === "text" || tag === "binding" || node.type === "comment") {
        return [];
      }
      const renderTag = findMappedTag2(node, documentMeta.tags);
      const components2 = [];
      if (node.type !== "root" && !htmlTags.has(renderTag)) {
        components2.push([tag, renderTag]);
      }
      for (const child of node.children || []) {
        components2.push(...loadComponents(child, documentMeta));
      }
      return components2;
    }
    function findMappedTag2(node, tags2) {
      const tag = node.tag;
      if (!tag || typeof node.props?.__ignoreMap !== "undefined") {
        return tag;
      }
      return tags2[tag] || tags2[pascalCase(tag)] || tags2[kebabCase(node.tag)] || tag;
    }
    return (_ctx, _push, _parent, _attrs) => {
      if (!isEmpty.value) {
        _push(ssrRenderComponent(MDCRenderer, mergeProps({
          body: body.value,
          data: data.value,
          class: props.class,
          tag: props.tag,
          prose: props.prose,
          unwrap: props.unwrap,
          components: componentsMap.value,
          "data-content-id": unref(debug) ? __props.value.id : void 0
        }, _attrs), null, _parent));
      } else {
        ssrRenderSlot(_ctx.$slots, "empty", {
          body: body.value,
          data: data.value,
          dataContentId: unref(debug) ? __props.value.id : void 0
        }, null, _push, _parent);
      }
    };
  }
};
const _sfc_setup$1 = _sfc_main$1.setup;
_sfc_main$1.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("../node_modules/@nuxt/content/dist/runtime/components/ContentRenderer.vue");
  return _sfc_setup$1 ? _sfc_setup$1(props, ctx) : void 0;
};
const __nuxt_component_2 = Object.assign(_sfc_main$1, { __name: "ContentRenderer" });
const _sfc_main = {
  __name: "[...slug]",
  __ssrInlineRender: true,
  async setup(__props) {
    let __temp, __restore;
    const route = useRoute();
    const { data: page } = ([__temp, __restore] = withAsyncContext(() => useAsyncData(
      `content-${route.path}`,
      () => queryCollection("content").path(route.path).first()
    )), __temp = await __temp, __restore(), __temp);
    if (!page.value) {
      throw createError({
        statusCode: 404,
        statusMessage: "Page not found",
        fatal: true
      });
    }
    const sections = computed(() => {
      const children = bodyChildren(page.value?.body);
      const result = [];
      let currentSection = null;
      for (const child of children) {
        if (nodeTag(child) === "h2") {
          currentSection = {
            id: nodeProps(child)?.id ?? `section-${result.length}`,
            heading: child,
            children: []
          };
          result.push(currentSection);
          continue;
        }
        if (!currentSection) {
          currentSection = {
            id: "intro",
            heading: null,
            children: []
          };
          result.push(currentSection);
        }
        currentSection.children.push(child);
      }
      return result;
    });
    function bodyChildren(body) {
      return body?.children ?? body?.value ?? [];
    }
    function nodeTag(node) {
      return Array.isArray(node) ? node[0] : node?.tag;
    }
    function nodeProps(node) {
      return Array.isArray(node) ? node[1] : node?.props;
    }
    function sectionValue(children) {
      return {
        ...page.value,
        body: bodyWithChildren(page.value.body, children)
      };
    }
    function bodyWithChildren(body, children) {
      if (body?.value) {
        return {
          ...body,
          value: children
        };
      }
      return {
        ...body,
        children
      };
    }
    return (_ctx, _push, _parent, _attrs) => {
      const _component_Banner = __nuxt_component_0;
      const _component_ContentBar = __nuxt_component_1;
      const _component_ContentRenderer = __nuxt_component_2;
      if (unref(page)) {
        _push(`<div${ssrRenderAttrs(mergeProps({ class: "flex flex-1 flex-col" }, _attrs))}>`);
        if (unref(page)) {
          _push(ssrRenderComponent(_component_Banner, {
            title: unref(page).title,
            imgSrc: unref(page).meta?.banner
          }, null, _parent));
        } else {
          _push(`<!---->`);
        }
        _push(`<div class="content-layout bg-primary-deep flex h-full flex-1 flex-row p-10 text-white lg:pl-0"><aside class="contents">`);
        _push(ssrRenderComponent(_component_ContentBar, {
          class: "hidden lg:block",
          toc: unref(page).body.toc.links
        }, null, _parent));
        _push(`</aside><main class="flex flex-1 flex-col gap-6"><!--[-->`);
        ssrRenderList(unref(sections), (section) => {
          _push(`<section class="flex flex-col gap-4">`);
          if (section.heading) {
            _push(ssrRenderComponent(_component_ContentRenderer, {
              value: sectionValue([section.heading]),
              class: "content flex-1 text-white"
            }, null, _parent));
          } else {
            _push(`<!---->`);
          }
          if (section.children.length) {
            _push(ssrRenderComponent(_component_ContentRenderer, {
              value: sectionValue(section.children),
              class: "content text-primary-deep flex-1 rounded-4xl bg-white p-6"
            }, null, _parent));
          } else {
            _push(`<!---->`);
          }
          _push(`</section>`);
        });
        _push(`<!--]--></main></div></div>`);
      } else {
        _push(`<!---->`);
      }
    };
  }
};
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("pages/[...slug].vue");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ____slug_ = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: _sfc_main
}, Symbol.toStringTag, { value: "Module" }));
export {
  ____slug_ as _,
  flatUnwrap as f,
  useHashScroll as u
};
//# sourceMappingURL=_...slug_-C36b5rf0.js.map
